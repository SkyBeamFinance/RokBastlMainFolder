# PDF Extractor

This folder contains tools to extract text from PDF files locally.

## Usage
To extract text from a PDF, you can use the provided script `extract.sh` or the Python script `extract.py`.
